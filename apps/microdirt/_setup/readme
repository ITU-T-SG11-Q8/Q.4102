# requirements.txt 를 활용한 Package 설치
pip install -r requirements.txt

# Package 설치
pip install requests
pip install schedule
pip install pymysql

# 실행 명령어
data_collector_run.py -ip=[전송 대상 IP] -port=[전송 대상 Port]
예) python data_collector_run.py -ip=127.0.0.1 -port=9001